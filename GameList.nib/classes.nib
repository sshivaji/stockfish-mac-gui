{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {closeGameFile = id; loadGame = id; }; 
            CLASS = GameListController; 
            LANGUAGE = ObjC; 
            OUTLETS = {gameList = id; }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}